/* Put this code replaced with your information into a file called firebase-Config_template. Put it in the same directory */

/*
import { initializeApp } from "firebase/app";
import {getAuth} from 'firebase/auth'
import { getFirestore, collection, getDocs } from 'firebase/firestore';

const firebaseConfig = {
  apiKey: "Your API key",
  authDomain: "Your auth domain",
  projectId: "Your project ID",
  storageBucket: "Your storage bucket",
  messagingSenderId: "Your messaging sender ID",
  appId: "Your API ID"
};

const app = initializeApp(firebaseConfig);
export const auth = getAuth(app); // Firebase Authentication instance
export const db = getFirestore(); // Firestore instance
*/